# reflog-website

This site is built with **Next.js + Nextra** and statically exported into `dist/` for GitHub Pages.

## Development

```bash
npm install
npm run dev
```

## Build (outputs to `dist/`)

```bash
npm run build
```

